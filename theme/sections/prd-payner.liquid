

  <script>
    /* 1612733201468 */

    if (!window.store.sections) window.store.sections = {};

    var sectionKey = '{{section.id}}';

    window.store.sections[sectionKey] = {
      settings: {
        section: 'slider',
        id: '{{section.id}}',
                  'enabled': {{ section.settings.enabled }},
              },
      blocks: {
                  {% assign blocks_to_loop = section.blocks | where: 'type', 'slide' %}
          'slide': [
            {%- for block in blocks_to_loop -%}
              {
                type: "{{block.type | escape}}",
                                  'title': 
            "{{block.settings.title | escape}}"
          ,
                                  'dingo': 
            "{{block.settings.dingo | escape}}"
          ,
                                  'image': 
            "{{block.settings.image | img_url:'400x400', crop:'center' }}"
          ,
                              },
            {%- endfor -%}
          ],
                  {% assign blocks_to_loop = section.blocks | where: 'type', 'bing' %}
          'bing': [
            {%- for block in blocks_to_loop -%}
              {
                type: "{{block.type | escape}}",
                                  'image2': 
            "{{block.settings.image2 | img_url:'master'}}"
          ,
                              },
            {%- endfor -%}
          ],
              },
    };
  </script>


{% schema %}    
      {
  "name": "slider",
  "settings": [
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "Enaererrr!wwww?",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "id": "title",
          "type": "text",
          "label": "Title"
        },
        {
          "id": "dingo",
          "type": "textarea",
          "label": "Big text"
        },
        {
          "id": "image",
          "type": "image_picker",
          "label": "Image"
        }
      ]
    },
    {
      "type": "bing",
      "name": "Bing",
      "settings": [
        {
          "id": "image2",
          "type": "image_picker",
          "label": "Image"
        }
      ]
    }
  ]
}  {% endschema %}